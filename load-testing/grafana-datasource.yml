apiVersion: 1

datasources:
  - name: InfluxDB-k6
    type: influxdb
    access: proxy
    url: http://influxdb:8086 # URL interna do Docker
    database: k6-payments
    jsonData:
      httpMode: GET